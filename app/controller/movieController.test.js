const { getAllMovies, getMoviesById } = require('./movieController');

jest.mock('./movieController');

describe('movieController Test', () => {
    test('', async () => {

    });

    test('', async () => {

    });
});