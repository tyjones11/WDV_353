const add = (str) => {
    return str.toAdd();}

const subtract = (str) => {
    return str.toSubtract();}

const multiply = (str) => {
    return str.toMultiply();}

const divide = (str) => {
    return str.toDivide();}

const squareRoot = (str) => {
    return str.toSquareRoot();}

const max = (str) => {
    return str.toMax();}

module.exports = {
    add,
    subtract,
    multiply,
    divide,
    squareRoot,
    max
}